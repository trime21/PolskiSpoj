public class Char {
    
    char getChar(boolean charTest) {
        char charUsed;
        if (charTest)
            charUsed = '*';
        else
            charUsed = '.';
        return charUsed;
    }
}
