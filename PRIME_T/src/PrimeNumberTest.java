public class PrimeNumberTest {
    static String value (boolean primeTest){
        String primeResult;
        if (primeTest)
            primeResult = "TAK";
        else
            primeResult = "NIE";
        return primeResult;
    }
}